{
  "name": "job-finder-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter next-app dev",
    "dev:webpack": "pnpm --filter next-app exec next dev --webpack",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter next-app test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3"
  }
}
