/** Shared eyebrow/section label style for consistent UI across cards and panels. Matches former .eyebrow (letter-spacing 0.1em). */
export const EYEBROW_CLASS =
  "text-xs uppercase tracking-[0.1em] text-muted-foreground";

/** Bottom margin for eyebrow headings (section labels). */
export const EYEBROW_MB = "mb-2";
